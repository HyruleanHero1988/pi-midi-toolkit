# Moved

The PiDI kiosk UI now lives at **[`apps/pidi`](../apps/pidi)**.

```bash
cd apps/pidi
export PYTHONPATH="$PWD"
python -m pidi --fullscreen
```

This folder is kept so old bookmarks and docs that mention `tools/midi-tone`
have somewhere obvious to land.
