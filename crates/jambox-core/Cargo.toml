[package]
name = "jambox-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Jambox soft-synth DSP and sample-accurate sequencer (no I/O, no alloc on the audio path)"

[dependencies]
midi-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
