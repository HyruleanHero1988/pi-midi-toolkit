[package]
name = "jambox-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Versioned wire types shared by jambox-engine and native PiDI clients"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
