# Archived one-offs

Bring-up helpers used while wiring TFT70 touch, GPIO IRQ, and session recovery.
They are **not** part of the kiosk runtime and are not copied by `deploy_pi.py`.

Prefer the documented scripts in the deploy root (`enable-gpio-touch.sh`,
`fix-touch-x11.sh`, `prefer-tft70-display.sh`, …) for anything still needed.
